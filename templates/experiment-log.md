# 实验记录｜复制后填写

实验名称：
日期：
对应学习阶段：

## 1. 问题与预测

我想检验的问题：

我预测会出现的结果，以及理由：

本次只改变的主变量：

## 2. 这次到底在做什么？

选择并说明：从零预训练 / SFT / RL 参数更新 / 推理 / Agent 轨迹与评测 / 其他。

是否执行反向传播和优化器更新：
模型来自随机初始化，还是已有 checkpoint：
本次不能声称完成的事情：

## 3. 可复现配置

| 项目 | 本次记录 |
|---|---|
| 代码仓库与 commit SHA | |
| Python / PyTorch / 关键依赖版本 | |
| 操作系统、GPU、显存、RAM | |
| 模型、tokenizer、chat template 版本 | |
| 数据来源与授权 | |
| 训练/验证/测试切分方式 | |
| 数据样本数或 token 数 | |
| 模型层数、宽度、注意力头数 | |
| 序列长度、微批量、梯度累积、并行设备数 | |
| 优化步骤、学习率、优化器 | |
| 随机种子 | |
| 推理温度、采样设置 | |
| RL 每题 rollout 数、评分规则 | |
| 最大时间/步骤/费用限制 | |
| checkpoint 与日志位置 | |

没有使用的项目填“不适用”，不要留成可以误读为默认值的空白。

## 4. 对照是否公平？

基线是哪一次实验：
保持不变的条件：
按相同步数、token 数还是运行时间比较：
是否反复使用测试集调参：
其他可能影响结果的因素：

## 5. 结果

| 指标 | 基线 | 本次 | 解释与限制 |
|---|---:|---:|---|
| 训练 loss | | | |
| 验证 loss / 固定任务成功率 | | | |
| 吞吐量 | | | |
| 峰值显存/内存 | | | |
| 总运行时间 | | | |
| 生成长度 / 格式失败率（适用时） | | | |

固定提示或测试题：

训练前输出：

训练后输出：

是否重新加载 checkpoint 验证：

## 6. 失败与排错

原始错误/现象：
最小复现步骤：
尝试过的单项修改：
实际解决原因与证据：
仍未解释的问题：

不要把未查明的原因写成确定结论。

## 7. 我的解释

这次模型接收了什么？

什么量真正发生了变化？

我凭什么判断变化有用？

结果是否支持原先预测？有哪些更简单的解释？

下一次只做哪一个最小实验？

## 8. AI 使用记录

自己先完成的理解/代码/预测：
AI 提供的帮助：
哪些建议经过我独立运行或推导验证：
我现在仍无法独立解释的部分：

## 提交前检查

删除 API Key、Token、私人路径和原始私密内容；不上传大权重和未清理轨迹。只保留理解结果必需的代码、配置、摘要和图表。不要因为得到一个好看的例子，就把它当作稳定能力证明。
